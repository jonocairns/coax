# Agent Instructions

- Use the default Nix development shell for repository tooling.
- Run one-off tools with `nix develop --command <command>`.
- Run GitHub CLI commands with `nix develop --command gh`.
- Check the Nix-provided `gh` before reporting it missing or unauthenticated.
